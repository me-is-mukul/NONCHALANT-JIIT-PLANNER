# Frontend environment variables (used at build time by Vite)
# Replace with your production/back-end URLs when building the frontend.

VITE_NODE_API=http://localhost:3000
VITE_PY_API=http://localhost:8000
