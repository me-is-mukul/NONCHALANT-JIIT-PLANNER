## To fuckin run it locally  (if u have docker)
```bash
docker compose build
docker compose up -d
```

## For niggers without docker
```bash
cd backend
npm i
npm run dev
```
```bash
cd backend
python -m venv venv
# activate the venv according to your os
pip install -r requirements.txt
uvicorn server:app --reload --port 8000
```
```bash
cd frontend
npm i
npm run dev
# follow the directed port
```